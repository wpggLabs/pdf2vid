name: Release
on:
  workflow_dispatch:
  push:
    tags: ["v*"]
permissions:
  contents: write
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        # macos-latest is Apple Silicon; macos-13 is Intel. fail-fast is
        # off, so if one platform's FFmpeg fetch fails the others still ship.
        platform: [windows-latest, macos-latest, macos-13, ubuntu-22.04]
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - uses: dtolnay/rust-toolchain@stable

      - name: Linux system deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf curl unzip

      - name: macOS fetch tools
        if: runner.os == 'macOS'
        run: brew install curl unzip xz

      - name: Fetch FFmpeg sidecar
        shell: bash
        # `host` lets the fetch script pick the correct target for the
        # runner (win-x86_64, mac-arm64/x86_64, or linux-x86_64).
        run: npm run fetch-ffmpeg

      - run: npm ci
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: pdf2vid v__VERSION__
          releaseDraft: true
          prerelease: false

  ci:
    needs: build
    if: always()
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Build matrix result: ${{ needs.build.result }}"
      - if: needs.build.result != 'success'
        run: |
          echo "One or more platform builds failed"
          exit 1